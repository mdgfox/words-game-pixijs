import { Texture } from "pixi.js";

export interface GameAssets {
    letterCell: Texture;
    letterCellGreen: Texture;
    buttonGreen: Texture;
    letterPick: Texture;
    letterPickPink: Texture;
}
